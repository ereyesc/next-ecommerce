import category from './category'
import heroImages from './heroImages'
import logoImage from './logoImage'
import product from './product'

export const schemaTypes = [product, category, heroImages, logoImage]
